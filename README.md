# Project Documentation SAE astrophoto 2026 

## Installation


### Virtual Environment

It is recommended to create a virtual environment before installing dependencies:

```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## Requirements

- Python 3.8+
- See `requirements.txt` for full dependency list


### Dependencies
```bashDocumentation SAE astrophot
pip install -r requirements.txt
```

Or install dependencies manually:
```bash
pip install [package-name]
```

## Usage


pour lancer le programme en ligne de commande (sans interface graphique): 

```bash
python -m Logique.ligneCommande
```

pour lancer le programme avec l'interface graphique : 

```bash
python ./main.py
``` 

## fait par : 

- Maxence LAURENCE 
- Mathys LEVITRE 
- Neda KHELIFI 